/**
 * The Gender enum is used for calculations that distinguish between male and
 * female subjects.
 */
export enum Gender {
  /**
   * Subject is male.
   */
  Male,
  /**
   * Subject is female.
   */
  Female,
}
